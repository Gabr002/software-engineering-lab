function AddTasks(props) { // props é um objeto que contém todas as propriedades passadas para o componente AddTasks. Ele permite que você passe dados de um componente pai para um componente filho.
    // console.log(props); // Isso imprime no console do navegador o objeto props, permitindo que você veja quais propriedades foram passadas para o componente.   
    return (
      <h1>Adicionar Tarefas</h1>  
    );
}
  
export default AddTasks;